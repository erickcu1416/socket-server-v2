# SERVER INIT

Reconstruir modulos de Node
```
npm install
```

Generar el DIST
```
npm run compile
tsc -w
```

Levtantar el servidor, cualquiera de estos comandos
```
npm run build test
nodemon dist/
node dist/
```